enum POST_TYPE {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
}

enum POST_STATUS {
  DRAFTED = 'drafted',
  DELETED = 'deleted',
  SCHEDULED = 'scheduled',
  PUBLISHED = 'published',
}

export { POST_TYPE, POST_STATUS };
