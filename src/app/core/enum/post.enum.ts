export enum E_POST_STATUS {
  SAVED = 'saved',
  DELETED = 'deleted',
  SCHEDULED = 'scheduled',
  PUBLISHED = 'published',
}

export enum E_POST_TYPE {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
}
