export enum EPostStatus {
  DELETED = 'deleted',
  PUBLISHED = 'published',
  SAVED = 'saved',
  SCHEDULED = 'scheduled',
}

export enum EPostType {
  IMAGE = 'image',
  TEXT = 'text',
  VIDEO = 'video',
}
