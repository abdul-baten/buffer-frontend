import { EventPropagationDirective } from './event-propagation.directive';

describe('EventPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new EventPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
